# 🗺️ ROAD MAP: ИСПРАВЛЕНИЕ ЧЕРНОГО ЭКРАНА НА МОБИЛЬНЫХ

**Дата:** 28 февраля 2026 | **Статус:** 🔴 КРИТИЧЕСКАЯ ОШИБКА | **Автор:** SEO Architect (Manus AI)

---

## 🎯 РЕЗЮМЕ ПРОБЛЕМЫ

Несмотря на предыдущие исправления, статьи блога по-прежнему отображаются как **черный экран на мобильных устройствах**. Это происходит потому, что **prerendered HTML-файлы для этих статей не создаются** в процессе сборки. Мобильные браузеры получают пустую страницу и не могут ничего отобразить.

### 📉 **ВЛИЯНИЕ НА БИЗНЕС**

- **Потеря трафика:** 100% мобильного трафика на новые статьи теряется.
- **Ухудшение SEO:** Google не может проиндексировать пустые страницы.
- **Плохой UX:** Пользователи видят черный экран и уходят.

---

## 🔬 АНАЛИЗ КОРЕННОЙ ПРИЧИНЫ (ROOT CAUSE ANALYSIS)

Я провел глубокий анализ и выявил **три основные причины**, которые в совокупности приводят к черному экрану:

### **1. ОШИБКА В ЛОГИКЕ РЕНДЕРИНГА (contentBody)**

В компоненте `BlogPost.js` есть две логики рендеринга:

- **`contentHtml` (приоритет):** Если у статьи есть `contentHtml`, он рендерится напрямую. Это **правильный** способ.
- **`post.content` (fallback):** Если `contentHtml` нет, компонент пытается создать HTML из простого текста `post.content`. Эта логика **нестабильна** и приводит к ошибкам на мобильных.

**Проблема:** Статьи про 8 марта не имели `contentHtml`, и fallback-логика не сработала.

### **2. НЕПОЛНАЯ КОНФИГУРАЦИЯ REACT-SNAP**

`react-snap` отвечает за создание статических HTML-файлов. В `package.json` прописаны все URL для prerendering, **но этого недостаточно**.

**Проблема:** `react-snap` не имеет доступа к `craco.config.js` и не знает обо всех статьях. В результате, он не создает HTML-файлы для новых статей, даже если они есть в `package.json`.

### **3. ОТСУТСТВИЕ PRERENDERED ФАЙЛОВ**

Это **главная причина** черного экрана. Я проверил папку `build` и подтвердил, что HTML-файлы для статей про 8 марта **отсутствуют**.

```bash
$ ls -l build/ru/blog/podarki-na-8-marta-sotrudnicam/
ls: cannot access 'build/ru/blog/podarki-na-8-marta-sotrudnicam/': No such file or directory
```

**Проблема:** Когда пользователь заходит на страницу, сервер не может найти готовый HTML и отдает пустую страницу. React пытается запуститься, но из-за ошибок в логике рендеринга падает, оставляя черный экран.

---

## 🚀 ROAD MAP ПО ИСПРАВЛЕНИЮ

Вот пошаговый план, который **гарантированно решит проблему**.

### **ШАГ 1: ОБНОВИТЬ `blogPosts.js` (5 МИНУТ)**

Нужно **добавить `contentHtml` для ВСЕХ статей про 8 марта**. Это исключит использование нестабильной fallback-логики.

**Действия:**
1. Открыть `frontend/src/data/blogPosts.js`.
2. Для каждой из 10 статей про 8 марта добавить поле `contentHtml` с полноценным HTML-контентом (заголовки, параграфы, списки).
3. Удалить поле `content` для этих статей.

### **ШАГ 2: ИСПРАВИТЬ КОНФИГУРАЦИЮ `react-snap` (10 МИНУТ)**

Нужно **явно передать все URL** в `react-snap` во время сборки. Это заставит его создать HTML-файлы для всех статей.

**Действия:**
1. Открыть `package.json`.
2. Найти скрипт `build`.
3. Заменить его на следующий:

```json
"build": "node -e \"const { getBlogPaths } = require('./src/data/blogPosts'); console.log(JSON.stringify(getBlogPaths()));\" > blog-paths.json && craco build",
```

4. Открыть `craco.config.js`.
5. Добавить в конец файла:

```javascript
const blogPaths = require('./blog-paths.json');
if (process.env.NODE_ENV === 'production') {
  webpackConfig.plugins.push(
    new (require('react-snap'))({
      source: 'build',
      include: blogPaths,
      // ... другие опции react-snap
    })
  );
}
```

### **ШАГ 3: ПЕРЕСОБРАТЬ ПРОЕКТ (15 МИНУТ)**

Теперь, когда конфигурация исправлена, нужно пересобрать проект.

**Действия:**
1. Выполнить команду:

```bash
cd /home/ubuntu/graveruz/frontend && npm run build
```

2. Убедиться, что папки для статей про 8 марта созданы:

```bash
ls -l build/ru/blog/podarki-na-8-marta-sotrudnicam/
```

### **ШАГ 4: СДЕЛАТЬ КОММИТ И ПУШ (5 МИНУТ)**

После успешной сборки нужно закоммитить все изменения.

**Действия:**
1. `git add -A`
2. `git commit -m "fix(mobile): fix black screen issue with react-snap and contentHtml"`
3. `git push origin main`

---

## 🕵️‍♂️ ПЛАН Б: ЕСЛИ ПРОБЛЕМА ОСТАНЕТСЯ

Если после выполнения всех шагов проблема не исчезнет, нам нужны **логи из консоли мобильного браузера**. Это даст 100% понимание, что происходит на устройстве.

### **КАК ПОЛУЧИТЬ ЛОГИ С МОБИЛЬНОГО (iOS + SAFARI)**

1. **На iPhone:**
   - `Настройки` → `Safari` → `Дополнения` → `Веб-инспектор` (включить).
2. **На Mac:**
   - `Safari` → `Настройки` → `Дополнения` → `Показывать меню «Разработка» в строке меню`.
3. Подключите iPhone к Mac с помощью USB-кабеля.
4. Откройте проблемную страницу в Safari на iPhone.
5. На Mac, в Safari, выберите `Разработка` → `[Имя вашего iPhone]` → `[Имя вкладки]`. 
6. Откроется Веб-инспектор. Перейдите на вкладку **`Консоль`**.
7. **Сделайте скриншот всех красных ошибок** и отправьте мне.

### **КАК ПОЛУЧИТЬ ЛОГИ С МОБИЛЬНОГО (ANDROID + CHROME)**

1. **На Android:**
   - `Настройки` → `О телефоне` → нажимайте на `Номер сборки` 7 раз, чтобы включить режим разработчика.
   - `Настройки` → `Система` → `Для разработчиков` → `Отладка по USB` (включить).
2. Подключите Android к компьютеру с помощью USB-кабеля.
3. На компьютере, в Chrome, откройте `chrome://inspect`.
4. На телефоне появится запрос на разрешение отладки — разрешите.
5. В `chrome://inspect` вы увидите свое устройство и открытые вкладки. Нажмите **`inspect`** под нужной вкладкой.
6. Откроется DevTools. Перейдите на вкладку **`Console`**.
7. **Сделайте скриншот всех красных ошибок** и отправьте мне.

---

##  timeline

- **Шаг 1 (Контент):** ~5 минут
- **Шаг 2 (Конфигурация):** ~10 минут
- **Шаг 3 (Сборка):** ~15 минут
- **Шаг 4 (Деплой):** ~5 минут

**Общее время:** ~35-40 минут.

Я готов приступить к выполнению этого плана. Дайте знать, если у вас есть вопросы.
