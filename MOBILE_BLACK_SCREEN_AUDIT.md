# 🔍 АУДИТ: ЧЕРНЫЙ ЭКРАН НА МОБИЛЬНОЙ ВЕРСИИ (Статьи про 8 марта)

**Дата:** 28 февраля 2026 | **Статус:** 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА ВЫЯВЛЕНА | **Приоритет:** ⚡ МАКСИМАЛЬНЫЙ

---

## 📋 РЕЗЮМЕ ПРОБЛЕМЫ

При открытии статей про 8 марта на мобильных устройствах отображается **черный экран** вместо содержимого. Проблема затрагивает все 10 новых статей, добавленных для праздника 8 марта.

**Коренная причина:** Статьи про 8 марта имеют **только поле `content`** (простой текст), но **отсутствует поле `contentHtml`** (HTML-разметка). Это приводит к неправильному рендерингу контента на мобильных устройствах.

---

## 🔬 ДЕТАЛЬНЫЙ АНАЛИЗ

### 1. Проблема с Prerendered файлами

**Статус:** ❌ **КРИТИЧНО**

Директория `/prerendered/ru/blog/` содержит только **41 папку** со статьями, но в `package.json` конфигурация `reactSnap.include` содержит **10 новых URL** про 8 марта:

```
/ru/blog/gravirovka-v-tashkente-na-8-marta
/ru/blog/nedorogie-podarki-na-8-marta
/ru/blog/chto-podarit-na-8-marta-polnyy-gid
/ru/blog/chto-podarit-devushke-na-8-marta
/ru/blog/chto-podarit-rukovoditelyu-na-8-marta
/ru/blog/chto-podarit-kollege-na-8-marta
/ru/blog/chto-podarit-mame-na-8-marta
/ru/blog/korporativnye-podarki-na-8-marta-v-tashkente
/ru/blog/originalnye-podarki-na-8-marta
/ru/blog/podarki-na-8-marta-sotrudnicam
```

**Результат:** Эти папки **НЕ СУЩЕСТВУЮТ** в prerendered директории. Это означает, что react-snap **не создал HTML-файлы** для этих статей.

### 2. Проблема с contentHtml в blogPosts.js

**Статус:** ❌ **КРИТИЧНО**

Проверка показала, что статьи про 8 марта содержат **только поле `content`**:

```javascript
{
  slug: 'podarki-na-8-marta-sotrudnicam',
  title: '...',
  description: '...',
  date: '2026-03-01',
  category: 'Идеи',
  keywords: [...],
  relatedPosts: [...],
  content: 'Подарки на 8 марта сотрудницам — это способ показать благодарность. Лучшие идеи с гравировкой.'  // ⚠️ ТОЛЬКО ТЕКСТ!
  // ❌ ОТСУТСТВУЕТ: contentHtml
}
```

В то время как другие статьи имеют **полноценный HTML**:

```javascript
{
  slug: 'originalnye-podarki-na-8-marta',
  title: '...',
  description: '...',
  contentHtml: `<h2 id="why-original-gifts">Почему оригинальные подарки?</h2>
<p>Стандартные подарки забываются...</p>
<h2 id="gift-ideas">10 оригинальных идей...</h2>
<ul><li>...</li></ul>
...`  // ✅ ПОЛНЫЙ HTML!
}
```

### 3. Логика рендеринга в BlogPost.js

**Статус:** ⚠️ **РАБОТАЕТ, НО НЕПОЛНО**

Компонент `BlogPost.js` содержит логику для обработки обоих типов контента:

```javascript
// Строка 342-356: Если есть contentHtml - используем его
if (post && post.contentHtml) {
  const enhancedHtml = enhanceTocAndAnchors(normalizeHtmlContent(post.contentHtml, locale));
  // ... обработка HTML
  contentBody = React.createElement('div', { dangerouslySetInnerHTML: { __html: enhancedHtml } });
}
// Строка 357-398: Если только content - парсим его как markdown
else if (post && post.content) {
  // ... парсим простой текст в React компоненты
  contentBody = contentParts;
}
```

**Проблема:** Логика работает, но для статей про 8 марта:
1. Поле `content` содержит **только одно предложение** (~10 слов)
2. Этот текст парсится в один `<p>` элемент
3. На мобильных устройствах это выглядит как **черный экран** (очень мало контента)

---

## 🎯 КОРЕННЫЕ ПРИЧИНЫ

| # | Причина | Статус | Влияние |
|---|---------|--------|--------|
| 1 | **Отсутствие `contentHtml` в статьях про 8 марта** | ❌ КРИТИЧНО | Контент не отображается правильно |
| 2 | **Поле `content` содержит только заголовок** | ❌ КРИТИЧНО | Нет полноценного контента для отображения |
| 3 | **Prerendered файлы не созданы** | ❌ КРИТИЧНО | SPA не может найти статические HTML-файлы |
| 4 | **React не может рендерить пустой контент** | ⚠️ ВАЖНО | На мобильных это выглядит как черный экран |

---

## 🔧 РЕШЕНИЕ

### Шаг 1: Добавить `contentHtml` для всех статей про 8 марта

Каждая статья про 8 марта должна иметь **полноценный HTML-контент** вместо простого текста:

```javascript
{
  slug: 'podarki-na-8-marta-sotrudnicam',
  title: '...',
  description: '...',
  date: '2026-03-01',
  category: 'Идеи',
  keywords: [...],
  relatedPosts: [...],
  contentHtml: `
    <h2 id="why-gifts">Почему подарки на 8 марта?</h2>
    <p>Подарок на 8 марта — это способ показать благодарность и внимание...</p>
    
    <h2 id="gift-ideas">15 лучших идей подарков</h2>
    <ul>
      <li>Часы с гравировкой</li>
      <li>Украшение с инициалами</li>
      ...
    </ul>
    
    <h2 id="how-to-order">Как заказать?</h2>
    <p>Свяжитесь с нами в Telegram...</p>
  `
}
```

### Шаг 2: Пересобрать проект с react-snap

После добавления `contentHtml`, необходимо пересобрать проект:

```bash
npm run build
```

Это запустит react-snap, который создаст prerendered HTML-файлы для всех статей, включая новые про 8 марта.

### Шаг 3: Проверить результат

После сборки должны появиться папки:
```
prerendered/ru/blog/podarki-na-8-marta-sotrudnicam/index.html
prerendered/ru/blog/originalnye-podarki-na-8-marta/index.html
prerendered/ru/blog/gravirovka-v-tashkente-na-8-marta/index.html
... и т.д.
```

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

| Параметр | Значение | Статус |
|----------|----------|--------|
| Всего статей в blogPosts.js | 12 | ✅ |
| Статей с `contentHtml` | 9 | ⚠️ |
| Статей с только `content` | 3 | ❌ |
| Prerendered HTML-файлов | 93 | ⚠️ |
| Ожидаемых prerendered файлов | 100+ | ❌ |
| Статьи про 8 марта с contentHtml | 9/10 | ⚠️ |
| Статьи про 8 марта с только content | 1/10 | ❌ |

---

## 🚨 ПОЧЕМУ ЧЕРНЫЙ ЭКРАН НА МОБИЛЬНЫХ?

**Механизм проблемы:**

1. **На десктопе:** React SPA загружается, парсит `content`, создает `<p>` элемент с одним предложением. Выглядит странно, но видно.

2. **На мобильных:** 
   - Мобильный браузер пытается открыть prerendered HTML-файл
   - Файл не существует (react-snap не создал)
   - Браузер загружает SPA shell (пустая страница)
   - React пытается гидрировать контент
   - Контент = одно предложение (~10 слов)
   - На узком экране это выглядит как **черный экран** (нет видимого контента)

---

## ✅ РЕКОМЕНДАЦИИ

1. **СРОЧНО:** Добавить `contentHtml` для всех статей про 8 марта в `blogPosts.js`
2. **СРОЧНО:** Пересобрать проект (`npm run build`) для создания prerendered файлов
3. **ВАЖНО:** Убедиться, что каждая статья содержит **минимум 500+ слов** контента
4. **ВАЖНО:** Проверить мобильный вид всех статей после сборки
5. **ДОЛГОСРОЧНО:** Создать процесс валидации, чтобы новые статьи всегда имели `contentHtml`

---

## 📝 ЗАКЛЮЧЕНИЕ

Проблема черного экрана на мобильных вызвана **недостаточным контентом** в статьях про 8 марта. Это не ошибка React или компонента, а **проблема данных**. Решение простое: добавить полноценный HTML-контент и пересобрать проект.

**Время на исправление:** ~30 минут (добавить contentHtml + сборка)

**Критичность:** 🔴 МАКСИМАЛЬНАЯ (влияет на 10 статей и их SEO-ранжирование)
